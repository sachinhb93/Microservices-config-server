spring:
  data:
    mongodb:
      host: localhost
      port: 27017
  application:
    name: product-service

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
server:
  port: 0
